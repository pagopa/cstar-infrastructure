<policies>
    <inbound>
        <set-variable name="calls" value="${calls}" />
        <set-variable name="period" value="${period}" />
        <include-fragment fragment-id="rate-limit-by-clientid-claim" />
    </inbound>
    <backend>
        <base />
    </backend>
    <outbound>
        <base />
    </outbound>
    <on-error>
        <base />
    </on-error>
</policies>